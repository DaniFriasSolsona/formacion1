def String VARIABLE_GLOBAL = "Soy una variable global"

pipeline{
    agent any
    stages{
      stage("Variable globales"){
        steps{
          script{
            echo "Cualquier stage puede imprimir a \"VARIABLE_GLOBAL\": ${VARIABLE_GLOBAL}"
          }
        }
      }
    }
}
