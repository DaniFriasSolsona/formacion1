pipeline{
    agent any
    stages{
    stage("DIR"){
            steps{
                script{
                    sh "pwd"
                    echo "${ WORKSPACE }"
                    sh "mkdir new-ruta"
                    dir("new-ruta")
                    {
                      sh "pwd"
                    }
                }
            }
        }
    }
}
