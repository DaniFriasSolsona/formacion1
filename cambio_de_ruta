pipeline{
    agent any
    stages{
    stage("DIR"){
            steps{
                script{
                    sh "pwd"
                    echo "${ WORKSPACE }"
                    sh "mkdir nueva-ruta"
                    dir("nueva-ruta")
                    {
                      sh "pwd"
                    }
                }
            }
        }
    }
}
